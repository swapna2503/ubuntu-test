name: Command via SSH

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install SSH key
      uses: shimataro/ssh-key-action@v2
      with:
       key: ${{ secrets.PRIVATEKEY }}
       known_hosts: ${{ secrets.SSHPRIVATEKEY }}
    - run: whoami
